[build]
  command   = "npm run build"
  publish   = "dist"

# SPA fallback — all unknown paths serve index.html so React handles routing
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
